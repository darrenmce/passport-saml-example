extends layout

block content
	div.hero-unit
		if user !== null
			h1 Hello, #{user.firstName}
			div.btn-group
				a.btn.btn-default(href="/profile") Profile
				a.btn.btn-default(href="/logout") Logout
